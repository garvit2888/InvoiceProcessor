import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "InvoiceFlow - Automated Invoice Processing",
  description: "Upload your invoice PDFs and automatically extract data to Google Sheets. Streamline your invoice management with InvoiceFlow.",
  keywords: ["invoice", "PDF", "automation", "Google Sheets", "data extraction"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        {children}
      </body>
    </html>
  );
}
